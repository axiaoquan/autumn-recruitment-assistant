# 隐私与数据边界

## 公开仓库包含什么

- 空白的 `index.html` 应用框架；
- 数据 Schema、接口文档和跨平台命令行脚本；
- 不含用户身份、投递记录、状态历史、面试内容或原始资料路径。

## 数据保存在哪里

页面数据保存在打开页面的浏览器中，优先使用 IndexedDB，不支持时回退到 `localStorage`。这些数据不会自动写入仓库，也不会发送到网络。

浏览器数据不是可靠的长期备份。完整 JSON 导出文件才是迁移浏览器或电脑时的恢复介质。

## 不应公开的内容

- 私用 HTML 实例；
- 页面导出的 JSON、CSV、Markdown；
- SQLite、旧系统备份、截图和抓取证据；
- 简历、联系方式、学校信息、投递状态和面经；
- 含用户名的本机绝对路径。

默认 `.gitignore` 会排除本项目约定的私用路径，但无法识别所有任意文件名。建议把个人文件统一放入 `private/`、`data/` 或 `exports/`。

## 发布前必须执行

```bash
node scripts/check.mjs
git status --short --ignored
git diff --cached
```

如果曾经错误提交过个人数据，仅从最新提交中删除并不够，因为旧提交仍可访问。此时不要继续推送，应重写本地 Git 历史；如果已经推送到公开仓库，还应立即删除或设为私有，并按泄露内容采取后续处理。
