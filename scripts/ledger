#!/bin/sh
set -eu

ledger_script_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
if command -v node >/dev/null 2>&1; then
  exec node "${ledger_script_dir}/ledger-cli.mjs" "$@"
fi

printf '%s\n' '错误：未找到 Node.js。请安装 Node.js 18 或更高版本；页面本身仍可直接使用。' >&2
exit 127
